%h1= @pin.title

= image_tag @pin.image.url(:medium)

.panel
  %p= @pin.description



.row
  .col-sm-6
    
    Submitted by 
    = @pin.user.email
  .col-sm-6
    %ul.nav.nav-pills.navbar-right
      %li= link_to "Back", root_path
      %li= link_to "Edit", edit_pin_path
      %li= link_to "Delete", pin_path, method: :delete, data: { confirm: "Are you sure you want to delete this pin?" }